{
  "name": "go-direct-estimate-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute go_direct_estimates --file=./migrations/0001_create_estimate_requests.sql --remote"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
